# Copy this file to .env.local and adjust if needed.
# Points the frontend at your local backend — the default is right
# for everyone running both frontend and backend on the same machine.
NEXT_PUBLIC_API_URL=http://localhost:8000
